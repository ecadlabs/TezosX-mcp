services:
  - type: web
    name: tezosx-mcp
    plan: free
    runtime: node
    rootDir: mcp
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: MCP_TRANSPORT
        value: http